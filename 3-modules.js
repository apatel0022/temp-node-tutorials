const names = require('./4 -modules');

const myFunc =  require('./5 - utilss');

const data = require('./6-alternative-flavor');
require('./7-mind-grenade');

//console.log(names);
//myFunc(names.name1);
//myFunc(names.name2);